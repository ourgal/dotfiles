{{ if eq .chezmoi.os "windows" "linux" "android" -}}
{{ if lookPath "chezmoi" -}}
encryption = "age"
[age]
    identity = "~/.ssh/id_ed25519"
    symmetric = true
[git]
    autoCommit = false
    autoPush = false
[gopass]
    mode = "builtin"
progress = true
{{ if eq .chezmoi.os "windows" -}}
[interpreter.ps1]
    command = "powershell"
    args = []
{{ if and (lookPath "pwsh") .my.modules.wt -}}
[cd]
    command = "pwsh"
{{ else }}
[cd]
    command = "cmd"
{{ end -}}
{{ end -}}
[edit]
    hardlink = false
{{ if lookPath "delta" -}}
[diff]
    command = "delta"
    args = ["-s", "{{ "{{" }} .Destination {{ "}}" }}", "{{ "{{" }} .Target {{ "}}" }}"]
{{ else if lookPath "difft" -}}
[diff]
    command = "difft"
    args = ["{{ "{{" }} .Destination {{ "}}" }}", "{{ "{{" }} .Target {{ "}}" }}"]
{{ else if lookPath "meld" -}}
[diff]
    command = "meld"
    args = ["--diff", "{{ "{{" }} .Destination {{ "}}" }}", "{{ "{{" }} .Target {{ "}}" }}"]
{{ end -}}
{{ end -}}
{{ end -}}
